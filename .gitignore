node_modules/
deploy/
.wrangler/

# Material original do designer (.ai/.eps/.psd, mockups, manual em PDF) —
# ~330MB de binários, não cabe bem no histórico do git. Fica só local;
# faça backup à parte (zip num Drive/Dropbox, por exemplo).
brand-assets/
